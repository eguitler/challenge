import styled from 'styled-components';

export const Container = styled.div`
  width: fit-content;
  position: absolute;
  right: 0;
  top: 0;
  z-index: -1;
  width: 100%;
  max-width: 60rem;
`;

export const Image = styled.img`
  width: 100%;
`;
